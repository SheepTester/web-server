{
  "name": "assassin",
  "version": "1.0.0",
  "description": "",
  "main": "server/app.js",
  "scripts": {
    "serve": "node server/start-server.js",
    "serve:loop": "git fetch origin master && git reset --hard origin/master && npm install && node server/app.js",
    "start": "node server/app.js",
    "test": "standard --verbose | snazzy"
  },
  "author": "Orbiit",
  "license": "MIT",
  "devDependencies": {
    "snazzy": "^8.0.0",
    "standard": "^14.3.1"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "ejs": "^3.0.1",
    "express": "^4.17.1",
    "forever-monitor": "^2.0.0",
    "lowdb": "^1.0.0"
  }
}
