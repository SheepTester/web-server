{
  "name": "web-server",
  "version": "1.0.0",
  "description": "",
  "main": "server/app.js",
  "scripts": {
    "serve": "npm run serve:update && node server/start-server.js",
    "serve:loop": "node server/server.js",
    "serve:update": "git fetch origin master && git reset --hard origin/master && npm install",
    "start": "node server/app.js",
    "test": "standard --verbose | snazzy"
  },
  "author": "SheepTester",
  "license": "MIT",
  "devDependencies": {},
  "dependencies": {
    "cors": "^2.8.5",
    "ejs": "^3.0.1",
    "express": "^4.17.1",
    "forever-monitor": "^2.0.0",
    "greenlock-express": "^4.0.3",
    "lowdb": "^1.0.0",
    "nodemailer": "^6.4.5"
  }
}
