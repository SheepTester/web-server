<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <title>Messages</title>
  </head>
  <body>
    <p>
      <em>
        <a href="?from=<%= messages[0] && messages[0].date.toISOString() %>&limit=<%= limit %>">Back in time</a>
      </em>
    </p>
    <% for (const { id, name, date, message } of messages) { %>
      <p data-time="<%= id %>" data-date="<%= date.toISOString() %>">
        <%= name %> says, <strong><%= message %></strong> <em>(<%= date.toString() %>)</em>
      </p>
    <% } %>
  </body>
</html>
