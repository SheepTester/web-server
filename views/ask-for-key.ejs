<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <title><%= key %></title>
    <meta name="description" content="<%= locals.description || `Give ${key}` %>" />
  </head>
  <body>
    <% if (locals.description) { %>
      <p><%= description %></p>
    <% } %>
    <form method="GET">
      <p><label><%= key %> <input type="text" name="key" autofocus></label></p>
      <input type="submit" value="OK">
    </form>
  </body>
</html>
